import { Box, colors } from "./common-components";

const Card = () => {
    return (
        <Box background={colors.mainColor} width="860px" height="540px">

        </Box>
    );
}

export default Card;
